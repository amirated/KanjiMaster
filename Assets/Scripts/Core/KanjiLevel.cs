namespace KanjiMaster.Core
{
    /// <summary>Which bundled dataset a session draws from. Selectable in code/data;
    /// no level-selection UI exists yet, so the default is N5.</summary>
    public enum KanjiLevel
    {
        N5,
        N4,
    }
}
